{
  "name": "timewarrior-recap",
  "scripts": {
    "test": "jest app/*.spec.js",
    "test:watch": "jest --watch app/*.spec.js",
    "build": "esbuild app/index.js --bundle --platform=node --minify --outfile=dist/timewarrior-recap.js"
  },
  "packageManager": "yarn@3.3.1",
  "dependencies": {
    "date-fns": "^2.29.3",
    "esbuild": "^0.17.4",
    "jest": "^29.3.1"
  }
}
